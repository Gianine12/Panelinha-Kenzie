import { IconButton } from '@material-ui/core'
import Edit from '@material-ui/icons/Edit'

const BtnEdit = () => {
  return (
    <IconButton style={{ padding: '0', paddingRight: '12px' }}>
      <Edit />
    </IconButton>
  )
}

export default BtnEdit
