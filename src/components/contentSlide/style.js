import styled from 'styled-components'
import { COLORS } from '../../styles/global'

const CardUserSlide = styled.div`
  padding: 20px 0px;
  i {
    font-size: 50px;
    margin: 0;
    color: ${COLORS.colorPrimary};
  }
  p {
    width: 100%;
    font-size: 12px;
    margin: 0;
  }
`
export default CardUserSlide
