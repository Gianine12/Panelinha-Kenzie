import IconButton from '@material-ui/core/IconButton'
import Settings from '@material-ui/icons/Settings'

const BtnEngre = () => {
  return (
    <IconButton>
      <Settings />
    </IconButton>
  )
}

export default BtnEngre
