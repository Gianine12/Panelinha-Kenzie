import styled from 'styled-components'
import { COLORS } from '../../styles/global'

const ListUsersSlide = styled.div`
  padding: 0px 20px;
  margin: 50px 0;
  border-top: 1px solid ${COLORS.colorGrey};
  border-bottom: 1px solid ${COLORS.colorGrey};
  border-width: 100%;
`
export default ListUsersSlide
